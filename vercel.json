{
  "version": 2,
  "buildCommand": "npx prisma generate && npx tsc --noEmit false",
  "outputDirectory": "dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
